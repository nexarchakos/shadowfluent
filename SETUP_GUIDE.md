# 🚀 Οδηγίες Προσθήκης OpenAI API Key

## Βήμα 1: Πάρε το OpenAI API Key

1. **Πήγαινε στο:** https://platform.openai.com/
2. **Κάνε login** (ή sign up αν δεν έχεις account)
3. **Πήγαινε στο:** https://platform.openai.com/api-keys
4. **Κάνε click στο "Create new secret key"**
5. **Δώσε όνομα** (π.χ. "English Shadowing App")
6. **Αντιγράψε το API key** - ⚠️ **Θα το δεις μόνο μια φορά!**

---

## Βήμα 2: Άνοιξε το .env File

### Επιλογή Α: Με Text Editor (π.χ. VS Code, TextEdit)

1. **Άνοιξε το VS Code** (ή οποιοδήποτε text editor)
2. **File → Open** (ή Cmd+O στο Mac)
3. **Πήγαινε στο folder:** `/Users/nicholasexarchakos/english-shadowing-app`
4. **Άνοιξε το αρχείο `.env`** (μπορεί να είναι κρυφό - πάτα Cmd+Shift+. για να δεις κρυφά αρχεία)

### Επιλογή Β: Με Terminal (πιο εύκολο!)

1. **Άνοιξε το Terminal**
2. **Τρέξε:**
   ```bash
   cd /Users/nicholasexarchakos/english-shadowing-app
   open -a TextEdit .env
   ```
   (ή `code .env` αν έχεις VS Code)

### Επιλογή Γ: Με Nano Editor (στο Terminal)

1. **Τρέξε:**
   ```bash
   cd /Users/nicholasexarchakos/english-shadowing-app
   nano .env
   ```
2. **Πάτα το API key** (Cmd+V για paste)
3. **Πάτα Ctrl+X** για exit
4. **Πάτα Y** για save
5. **Πάτα Enter** για confirm

---

## Βήμα 3: Προσθήκη του API Key

Στο `.env` file, θα δεις κάτι σαν αυτό:

```
VITE_OPENAI_API_KEY=your_openai_api_key_here
```

**Αντέγραψε το API key σου** (αυτό που αντιγράψες από το OpenAI) και **βάλτο αντί για `your_openai_api_key_here`**

**Παράδειγμα:**
```
VITE_OPENAI_API_KEY=sk-proj-abc123xyz789...
```

⚠️ **Σημαντικό:**
- **ΜΗΝ** βάλεις quotes (") γύρω από το API key
- **ΜΗΝ** βάλεις spaces πριν ή μετά το =
- Το API key ξεκινάει με `sk-`

---

## Βήμα 4: Αποθήκευση και Restart

1. **Αποθήκευσε το αρχείο** (Cmd+S)
2. **Σταμάτησε το dev server** αν τρέχει (Ctrl+C στο terminal)
3. **Ξανατρέξε:**
   ```bash
   npm run dev
   ```

---

## ✅ Έλεγχος αν Λειτουργεί

1. **Άνοιξε την εφαρμογή** στο browser
2. **Επίλεξε μια κατηγορία** (π.χ. Business)
3. **Πάτα "Generate 1 Phrase"**
4. **Αν δεις:**
   - ✅ **Νέα, μοναδική φράση** → Λειτουργεί!
   - ✅ **Badge "✨ AI Generated"** → Λειτουργεί!
   - ❌ **Μία από τις standard phrases** → Το API key δεν είναι σωστό

---

## 🔍 Troubleshooting

### Το API key δεν λειτουργεί

1. **Έλεγξε αν το API key είναι σωστό:**
   - Πρέπει να ξεκινάει με `sk-`
   - Πρέπει να είναι χωρίς quotes
   - Πρέπει να μην έχει spaces

2. **Έλεγξε αν έχεις credits:**
   - Πήγαινε στο https://platform.openai.com/account/billing
   - Βεβαιώσου ότι έχεις credits ή payment method

3. **Restart το dev server:**
   ```bash
   # Σταμάτα το server (Ctrl+C)
   npm run dev
   ```

### Δεν βρίσκω το .env file

Το `.env` file μπορεί να είναι κρυφό. Στο VS Code:
- **Cmd+Shift+P** → "Show Hidden Files"
- Ή πάτα **Cmd+Shift+.** για να δεις κρυφά αρχεία

Στο Terminal:
```bash
ls -la | grep .env
```

---

## 💰 Κόστος

- **Model:** `gpt-4o-mini` (οικονομικό)
- **Κόστος:** ~$0.00015 ανά φράση (πολύ φθηνό!)
- **Free Tier:** OpenAI δίνει $5 δωρεάν credits για νέους λογαριασμούς

---

## 📝 Παράδειγμα .env File

Το `.env` file σου θα πρέπει να μοιάζει κάπως έτσι:

```
VITE_OPENAI_API_KEY=sk-proj-abc123xyz789def456ghi012jkl345mno678pqr901stu234vwx567yz
```

**Μόνο αυτή η γραμμή!** Τίποτα άλλο.

---

## 🆘 Αν Χρειάζεσαι Βοήθεια

Αν έχεις πρόβλημα:
1. Άνοιξε το browser console (F12)
2. Κάνε click "Generate 1 Phrase"
3. Δες τα error messages
4. Στείλε μου το error message
